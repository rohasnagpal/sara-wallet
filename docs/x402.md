# x402 Agentic Payments

x402 lets Sara (or an agent using Sara) pay for machine-priced HTTP
resources automatically — no account, no API key, on either side.

1. **Pay-per-call HTTP fetches** — give Sara a URL; if it answers `402
   Payment Required`, Sara pays the exact USDC price and returns the
   resource.
2. **Policy-gated autonomous payment** — a spending policy scoped to a
   wallet/network (the same engine behind Sara's scoped spending controls,
   see [business-payments.md](business-payments.md)) lets payments under
   your cap go through with no passphrase prompt, so an agent can pay for
   calls unattended; anything outside the policy falls back to your
   passphrase, same as any other send.
3. **Trusted-asset only** — Sara only ever authorizes its own
   developer-verified USDC contract per network, never whatever asset
   address a 402 response itself claims.
4. **Base, Polygon, Ethereum, Arbitrum** for real payments, plus **Base
   Sepolia** for free testnet trials (no real money, not recorded in your
   ledger) — each network's USDC/EIP-3009 support independently confirmed
   against x402's own asset registry, not assumed.
5. **Demo seller + LLM agent included** — `examples/x402/` has a runnable
   multi-resource paid site (weather/trivia/stock/recipe, each a different
   price) with a free catalog, and a tool-calling agent that browses the
   catalog, picks the resource that matches its task, and pays for only
   that one — see `examples/x402/README.md`.

## Where the code lives

- `backend/app/tools/payments/x402_client.py` — the buyer-side client
- `backend/app/routers/x402.py` — API routes
- `examples/x402/` — the runnable demo seller and agent

## Security notes

See [security-model.md](security-model.md#can-the-ai-send-money-itself)
for how policy-gated auto-pay stays bounded to what you've explicitly
authorized in advance.

## Roadmap

Broadening seller/network coverage and policy-gating scenarios beyond the
demo is tracked in [../ROADMAP.md](../ROADMAP.md) (Now) —
[issue #3](https://github.com/rohasnagpal/sara-wallet/issues/3).
